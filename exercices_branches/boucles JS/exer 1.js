


for (let numero= 0; numero <= 99; numero++) { 
    console.log(numero);
  }


let n = -1;  // Inicializa la variable 'n' con valor -1 para que sea visible el 0
let x = 99; // Inicializa la variable 'x' con valor 99
while (n < 99) {  // Mientras 'n' sea menor que 99, ejecuta el bloque de código
  n++;           // Incrementa el valor de 'n' en 1
  x += n;        // Suma el valor de 'n' al valor de 'x'
  console.log(n); // Muestra en consola el valor actual de 'n'
}


for (let compteur=50; compteur <= 99; compteur++){
    console.log(compteur)
}

for(let alrevez=49; alrevez >= 1; alrevez--) {
    console.log(alrevez)
}